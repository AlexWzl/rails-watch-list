<h1><%= @list.name %></h1>
<%= pluralize(@list.movies.size, 'movie') %>
<%= "You haven't saved any movies yet!" if @list.movies.empty? %><br>
<%= link_to 'Delete this list', list_path(@list), method: :delete %>
